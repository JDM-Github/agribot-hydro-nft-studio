import type { CameraInfo } from "$types/cameraInfo";

export const CAMERA_INFO: CameraInfo = {
    status: 'false',
    resolution: 'NOT SET',
    fps: 0,
    ip: 'NOT SET',
    detectionConf: null
};

